# ZERO-DEFECT .npmrc (Single Source of Truth fuer npm-Verhalten)
# legacy-peer-deps: swagger-ui-react verlangt react <20, Projekt nutzt react 19
# -> npm ci wuerde sonst mit ERESOLVE abbrechen (lokal UND in CI)
legacy-peer-deps=true
fund=false
audit=false