import nextVitals from 'eslint-config-next/core-web-vitals';
import nextTypescript from 'eslint-config-next/typescript';

const eslintConfig = [
  // ─── IGNORED FILES ──────────────────────────────────────────
  {
    ignores: [
      "next-env.d.ts",
      ".next/**/*",
      "node_modules/**/*",
      "dist/**/*",
      "build/**/*",
      "*.config.js",
      "*.config.mjs",
    ],
  },
  // ─── NEXT.JS ESLINT KONFIGURATIONEN ────────────────────────
  ...nextVitals,
  ...nextTypescript,
];

export default eslintConfig;
